% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/matSPACE.R
\name{lambda.bound}
\alias{lambda.bound}
\title{Generate a log-spaced lasso penalty sequence for \code{\link[=space]{space()}}}
\usage{
lambda.bound(dt, eps = 1e-06, K = 30)
}
\arguments{
\item{dt}{list of n matrices, each p x q, in the same format expected
by the \code{data} argument of \code{\link[=space]{space()}}.}

\item{eps}{ratio of the smallest to the largest penalty in the
returned sequence, i.e. \code{lambda_min = lambda_max * eps}.}

\item{K}{number of penalty values to generate.}
}
\value{
A numeric vector of length \code{K}, decreasing geometrically from
\code{lambda_max} to \code{lambda_max * eps}, suitable to pass one at a time
as the \code{lam} argument of \code{\link[=space]{space()}} (e.g. selecting among the fits
with a BIC-type criterion).
}
\description{
Computes a decreasing, log-spaced sequence of \code{K} candidate lasso
penalties (\code{lam} values) for tuning \code{\link[=space]{space()}}, analogous to the
\code{lambda_max}-based grids used in lasso path algorithms. The largest
value, \code{lambda_max}, is the largest off-diagonal entry of a
variance-rescaled Gram matrix — the smallest penalty above which every
off-diagonal partial correlation coefficient is driven to zero; the
sequence descends geometrically to \code{lambda_max * eps}.
}
\examples{
set.seed(1)
p <- 5; q <- 4; n <- 3
data <- replicate(n, matrix(rnorm(p * q), p, q), simplify = FALSE)
lambda.bound(data, K = 10)
}
